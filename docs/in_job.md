# In Job: v1.1 - Полировка релизной базы и доставки

Этот файл - рабочий список задач для Codex. Здесь фиксируем текущую версию в работе, шаги реализации, проверки и статус. После завершения v1.1 сюда можно добавить следующий блок v1.2 и продолжать вести файл как журнал разработки.

## Правила работы с файлом

- Все задачи новой версии сначала описываем здесь.
- Реализованные пункты отмечаем `[x]`.
- Если в процессе появилась новая подзадача, добавляем ее в нужный раздел.
- Если пункт переносится в следующую версию, отмечаем причину.
- После завершения версии фиксируем:
  - что сделано;
  - какие тесты пройдены;
  - где лежит сборка;
  - какой коммит/тег соответствует релизу.

## Статусы

- `[ ]` не начато
- `[~]` в работе
- `[x]` готово
- `[!]` заблокировано или требует решения

---

# v1.1 - Полировка релизной базы и доставки

## Цель версии

Сделать доставку программы аккуратной и понятной: пользователь скачивает актуальную версию с сайта/GitHub, видит номер версии в приложении, может открыть информацию о программе, а мы имеем понятный процесс сборки и публикации.

v1.1 не должна менять расчетную логику финансового отчета. Это сервисно-релизная версия: упаковка, версия, доставка, проверка, документация.

## Что должно стать лучше для пользователя

- Пользователь видит, какая версия программы у него установлена.
- На сайте и в приложении указана одна и та же версия.
- Скачивание идет через нормальный GitHub Release, а не через raw-файл из репозитория.
- В приложении есть окно `О программе`.
- В приложении есть ручная проверка новой версии или хотя бы понятная ссылка на страницу скачивания.
- Архив релиза имеет предсказуемое имя.
- Есть инструкция: как скачать, распаковать, активировать и запустить.

## Что должно стать лучше для разработки

- Версия хранится в одном месте в коде.
- Сборка exe использует версию из проекта.
- Landing и README легко обновлять под новую версию.
- Процесс релиза описан пошагово.
- `main` остается рабочей веткой разработки, `prod` - веткой проверенного релиза.

---

## Этап 1. Единый номер версии

### Задачи

- [x] Добавить единое место хранения версии приложения.
  - Возможные варианты:
    - `src/wb_finance_analyst/domain/constants.py`
    - или отдельный файл `src/wb_finance_analyst/version.py`
  - Рекомендуемый вариант: `version.py`, чтобы версию можно было импортировать без доменной логики.

- [x] Установить версию `1.1.0-dev` на время разработки.

- [x] Использовать версию в:
  - [x] заголовке приложения; окно `О программе` будет подключено на Этапе 2;
  - [x] Excel-метаданных и видимом листе настроек отчета;
  - [x] README;
  - [x] landing;
  - [x] имени релизного архива через `RELEASE_ARCHIVE_NAME`.

### Критерии готовности

- [x] В коде нет нескольких руками прописанных разных версий.
- [x] При смене версии понятно, какие файлы надо обновить.
- [x] Тесты проходят.

---

## Этап 2. Окно `О программе`

### Задачи

- [x] Добавить кнопку или пункт в интерфейсе `О программе`.
  - Вариант размещения:
    - внизу левого меню рядом с `Инструкция` и `Логи`;
    - или отдельная маленькая кнопка в нижней части сайдбара.

- [x] Создать диалог `AboutDialog`.

- [x] В диалоге показать:
  - [x] название `WB analyst`;
  - [x] логотип;
  - [x] версию;
  - [x] краткое описание;
  - [x] ссылку на сайт/страницу скачивания;
  - [x] предупреждение, что API-токены хранятся локально и не пишутся в логи;
  - [x] контакты/канал поддержки: продавец или разработчик, который выдал лицензию.

- [x] Добавить кнопку `Скопировать сведения`.
  - Должна копировать:
    - название;
    - версию;
    - путь к логам;
    - дату/время.

### Критерии готовности

- [x] Диалог открывается без зависаний.
- [x] Версия отображается корректно.
- [x] Копирование сведений работает.
- [x] UI не перегружен.

---

## Этап 3. Ручная проверка новой версии

### Задачи

- [ ] Решить формат источника последней версии.

Рекомендуемый простой вариант для v1.1:

- файл `latest.json` в репозитории или на GitHub Pages/Raw:

```json
{
  "version": "1.1.0",
  "download_url": "https://github.com/mdorovn-ux/wb_analist/releases/download/v1.1.0/WB-analyst-v1.1.0.zip",
  "notes_url": "https://github.com/mdorovn-ux/wb_analist/releases/tag/v1.1.0"
}
```

- [ ] Добавить кнопку `Проверить обновление` в `О программе`.

- [ ] При проверке:
  - [ ] не использовать WB API;
  - [ ] делать обычный HTTP GET к `latest.json`;
  - [ ] иметь короткий timeout;
  - [ ] не блокировать UI;
  - [ ] если версия новее - показать ссылку скачать;
  - [ ] если версия актуальная - показать `Установлена актуальная версия`;
  - [ ] если интернета нет - показать мягкую ошибку.

### Упрощение, если не хотим делать сетевую проверку в v1.1

Можно вместо проверки добавить кнопку `Открыть страницу скачивания`. Тогда автоматическую проверку перенести в v1.2/v1.3.

Решение перед реализацией:

- [ ] выбрать: `Проверить обновление` через `latest.json`;
- [ ] или выбрать: только `Открыть страницу скачивания`.

### Критерии готовности

- [ ] Проверка/открытие ссылки не влияет на WB API лимиты.
- [ ] При отсутствии интернета программа не зависает.
- [ ] Ошибка понятна пользователю.

---

## Этап 4. GitHub Releases вместо raw-ссылки

### Задачи

- [ ] Создать релиз `v1.1.0` в GitHub Releases после сборки.

- [ ] Прикрепить архив:

```text
WB-analyst-v1.1.0.zip
```

- [ ] Обновить landing-ссылку скачивания:

```text
https://github.com/mdorovn-ux/wb_analist/releases/download/v1.1.0/WB-analyst-v1.1.0.zip
```

- [ ] Обновить README:
  - [ ] ссылка на актуальный релиз;
  - [ ] инструкция установки;
  - [ ] инструкция активации;
  - [ ] инструкция запуска сайта на Railway.

- [ ] Решить, хранить ли ZIP внутри репозитория после перехода на GitHub Releases.

Рекомендация:

- для `v1.1` лучше убрать новые ZIP из git и держать их в GitHub Releases;
- старый `releases/WB-analyst-v1.0.zip` можно оставить как исторический файл или удалить отдельным решением.

### Критерии готовности

- [ ] Сайт скачивает файл из GitHub Releases.
- [ ] Ссылка работает в публичном режиме.
- [ ] Размер файла не мешает git-репозиторию.

---

## Этап 5. Улучшение landing под релизную доставку

### Задачи

- [ ] Обновить текст версии на сайте.

- [ ] Добавить короткий блок `Как установить`:
  - скачать ZIP;
  - распаковать папку;
  - запустить exe;
  - отправить код компьютера;
  - вставить ключ активации.

- [ ] Добавить блок `Что входит в v1.1`.

- [ ] Добавить предупреждение:
  - программа для Windows;
  - Python устанавливать не нужно;
  - API-ключи пользователь вставляет сам;
  - WB API лимиты могут влиять на время загрузки.

- [ ] Проверить адаптивность страницы на мобильной ширине.

### Критерии готовности

- [ ] Пользователь понимает, что скачивать и что делать после скачивания.
- [ ] Кнопка скачивания заметная.
- [ ] Ссылка ведет на актуальный GitHub Release.

---

## Этап 6. README и пользовательская инструкция

### Задачи

- [ ] Корневой `README.md`:
  - [ ] актуальная версия;
  - [ ] ссылка на скачивание;
  - [ ] как запустить landing;
  - [ ] как собрать приложение;
  - [ ] где roadmap;
  - [ ] как устроены ветки `main` и `prod`.

- [ ] `wb_finance_analyst/README.md`:
  - [ ] запуск из исходников;
  - [ ] сборка exe;
  - [ ] проверка тестов;
  - [ ] ручная активация;
  - [ ] калькулятор лицензии.

- [ ] `landing/README.md`:
  - [ ] Railway deploy;
  - [ ] Root Directory: `landing`;
  - [ ] Start Command: `npm start`;
  - [ ] как обновить ссылку скачивания.

- [ ] Инструкция внутри приложения:
  - [ ] убедиться, что она не содержит лишнюю информацию про активацию после входа в приложение;
  - [ ] добавить только полезные сведения по текущему функционалу.

### Критерии готовности

- [ ] По README можно собрать, запустить и задеплоить проект без дополнительных пояснений.

---

## Этап 7. Сборка и упаковка v1.1

### Задачи

- [ ] Обновить версию на финальную:

```text
1.1.0
```

- [ ] Запустить проверки:

```bash
cd wb_finance_analyst
python -m compileall .
python -m pytest
python main.py
```

- [ ] Собрать exe:

```bash
cd wb_finance_analyst
python -m PyInstaller --noconfirm --clean --windowed --paths src --name "WB analyst v1.1.0" --add-data "src\wb_finance_analyst\resources;wb_finance_analyst\resources" main.py
```

- [ ] Проверить exe:
  - [ ] запускается;
  - [ ] показывает правильную версию;
  - [ ] открывается `О программе`;
  - [ ] открываются `Автоотчет`, `Себестоимость товаров`, `Инструкция`, `Логи`;
  - [ ] закрывается без задержки;
  - [ ] не требует установленный Python.

- [ ] Упаковать:

```text
WB-analyst-v1.1.0.zip
```

### Критерии готовности

- [ ] ZIP содержит рабочую папку приложения.
- [ ] Exe запускается после распаковки.
- [ ] Архив готов к прикреплению в GitHub Release.

---

## Этап 8. Релиз в GitHub

### Задачи

- [ ] Commit в `main`:

```bash
git add .
git commit -m "Prepare WB analyst v1.1.0 release"
git push origin main
```

- [ ] Создать тег:

```bash
git tag v1.1.0
git push origin v1.1.0
```

- [ ] Создать GitHub Release `v1.1.0`.

- [ ] Прикрепить `WB-analyst-v1.1.0.zip`.

- [ ] Проверить скачивание с landing.

- [ ] После финальной проверки обновить `prod`:

```bash
git checkout prod
git merge main --ff-only
git push origin prod
git checkout main
```

### Критерии готовности

- [ ] `main` содержит v1.1.0.
- [ ] `prod` обновлен только после проверки.
- [ ] GitHub Release содержит архив.
- [ ] Landing скачивает архив из Release.

---

## Этап 9. Тест-план v1.1

### Автотесты

- [ ] `python -m compileall .`
- [ ] `python -m pytest`

### UI smoke

- [ ] `python main.py`
- [ ] открыть приложение;
- [ ] открыть `О программе`;
- [ ] проверить версию;
- [ ] открыть страницу скачивания/проверку обновления;
- [ ] открыть `Инструкция`;
- [ ] открыть `Логи`;
- [ ] закрыть приложение.

### Exe smoke

- [ ] запустить собранный exe;
- [ ] проверить окно активации, если лицензии нет;
- [ ] проверить запуск, если лицензия уже есть;
- [ ] проверить `О программе`;
- [ ] проверить закрытие;
- [ ] проверить, что логотип отображается.

### Landing

- [ ] `npm start`;
- [ ] открыть `http://localhost:3000`;
- [ ] проверить кнопку скачивания;
- [ ] проверить мобильную ширину;
- [ ] проверить Railway README.

### Release

- [ ] GitHub Release открыт;
- [ ] архив скачивается;
- [ ] архив распаковывается;
- [ ] exe запускается из распакованной папки.

---

## Открытые решения перед началом v1.1

- [ ] Делать полноценную проверку обновления через `latest.json` или только кнопку `Открыть страницу скачивания`?
- [ ] Оставлять ли старый ZIP `v1.0` в git после перехода на GitHub Releases?
- [ ] Что указать в `О программе` как контакт поддержки?
- [ ] Делать ли `GitHub Release` вручную через сайт или автоматизировать через `gh release create`?

---

## Итог v1.1

Заполнить после завершения:

- Статус: `[ ] не начато`
- Коммит main:
- Коммит prod:
- Тег:
- GitHub Release:
- Архив:
- Что сделано:
- Что перенесено:
- Какие проверки пройдены:
